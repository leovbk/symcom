{% extends 'base.html.twig' %} {% block title %}Catégories{% endblock %} {%
block body %}

<div class="container py-5">
  <ul class="list-group list-group-light">
    {% for c in categories.findAll() %}
    <li class="list-group-item px-3 border-0 text-center active">
      <a
        class="nav-link"
        href="{{ path('app_product_category', { id: c.id }) }}"
        >{{ c.name }}</a
      >
    </li>
    {% endfor %}
  </ul>
</div>

{% endblock %}
