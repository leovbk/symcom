<li class="nav-item dropdown">
  <a
    class="nav-link dropdown-toggle"
    data-bs-toggle="dropdown"
    href="{{ path('app_category') }}"
    role="button"
    aria-haspopup="true"
    aria-expanded="false"
    >Catégories</a
  >
  <div class="dropdown-menu">
    <a class="dropdown-item" href="{{ path('app_category') }}"
      >Toutes les catégories</a
    >
    {% for c in categories.findAll()%}
    <a
      class="dropdown-item"
      href="{{ path('app_product_category', { id: c.id }) }}"
      >{{ c.name | upper }}</a
    >

    {% endfor %}
  </div>
</li>
