<div class="ghost" style="min-height: 8vh !important"></div>
<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_home') }}">Symcom</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarColor01"
      aria-controls="navbarColor01"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('app_home') }}">Accueil </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('app_product') }}">Nos produits</a>
        </li>
        {% include "_incs/_cat_nav.html.twig" %}
      </ul>
      <form class="d-flex">
        <input class="form-control me-sm-2" type="text" placeholder="Search" />
        <button class="btn btn-secondary my-2 my-sm-0" type="submit">
          Search
        </button>
      </form>
      <nav class="navbar">
        <ul class="navbar-nav navbar-right">
          {% if not app.user %}
          <li class="nav-item">
            <a href="{{ path('app_login') }}" class="nav-link"
              ><i class="fa-solid fa-arrow-right-to-bracket"></i
              ><small class="text-muted"> Connection</small></a
            >
          </li>
          {% else %}
          <a href="{{ path('app_cart') }}" class="nav-link"
            ><i class="fa-solid fa-user"></i
            ><small class="text-muted"> Panier</small></a
          >
          <li class="nav-item">
            <a href="{{ path('app_logout') }}" class="nav-link"
              ><i class="fa-solid fa-power-off text-danger"></i
            ></a>
          </li>
          {% if is_granted('ROLE_USER') %}
          <li class="nav-item">
            <a href="#" class="nav-link"
              ><i class="fa-solid fa-user"></i
              ><small class="text-muted"> Profil</small></a
            >
          </li>
          {% endif %} {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item">
            <a href="{{ path('app_admin') }}" class="nav-link"
              ><i class="fa-solid fa-user"></i
              ><small class="text-muted">Admin</small></a
            >
          </li>
          {% endif %} {% endif %}

          <li class="nav-item">
            <a href="{{ path('app_register') }}" class="nav-link"
              ><small class="text-muted"> Inscription</small></a
            >
          </li>
        </ul>
      </nav>
    </div>
  </div>
</nav>
